

let user = {
    firstName: `Michael`, // DIGITE O NOME DO USUÁRIO
    secondName: `Jackson`, // DIGITE O SOBRENOME DO USUÁRIO
    country: `Brazil` // DIGITE O PAÍS DE ORIGEM DO USUÁRIO
}

if (user.country === `Brazil`) {console.log(`${user.firstName} ${user.secondName} é Brasileiro.`)}
else {console.log(`${user.firstName} ${user.secondName} não é Brasileiro.`)}
